// Print-shop palette. Mirrors the CSS custom properties in index.css so the
// values are available to styled-components that prefer JS tokens.
const ink = '#17160f';
const inkSoft = '#3a382c';
const paper = '#ece6d6';
const paperLight = '#f6f1e6';
const lime = '#c9f74b';
const limeDeep = '#aee02f';
const coral = '#ff5a36';

const shadow = '5px 5px 0 #17160f';
const shadowLg = '8px 8px 0 #17160f';

// Back-compat alias for the old single accent.
const green = lime;

export {
    ink,
    inkSoft,
    paper,
    paperLight,
    lime,
    limeDeep,
    coral,
    shadow,
    shadowLg,
    green,
};
