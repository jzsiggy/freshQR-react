version: "3.8"

services:
  client:
    build: .
    env_file:
      - .env
    ports:
      - "3000:3000"
    # Keep the Create React App dev server alive inside the container.
    stdin_open: true
    tty: true
    restart: unless-stopped
